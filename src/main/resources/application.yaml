spring:
  application:
    name: libreria-api
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    org.springframework.security: DEBUG

jwt:
  secret:
    key: ${SECRET_KEY}