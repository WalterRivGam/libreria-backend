spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
jwt:
  secret:
    key: "una-clave-falsa-super-larga-y-segura-solo-para-que-spring-no-se-queje-en-las-pruebas"
